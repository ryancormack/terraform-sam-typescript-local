{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Debug API local",
            "port": 5858,
            "address": "localhost",
            "localRoot": "${workspaceFolder}",
            "remoteRoot": "/var/task/"
        }
    ]
}